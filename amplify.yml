version: 1
applications:
  - appRoot: my-portfolio
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .  # Pointing to the root where your static files are
        files:
          - '**/*'
